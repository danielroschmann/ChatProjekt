export default class User {
    constructor(id, navn, password, dato, niveau) {
        this.id = id;
        this.navn = navn;
        this.password = password;
        this.dato = dato;
        this.niveau = niveau;
    }
}