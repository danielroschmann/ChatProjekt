doctype HTML 
html 
    head 
        title Redigér #{chat.navn} 
    body 
        form(action=`/chats/${chat.id}/edit`, method="post") 
            input(type="text", name="newName", placeholder="Nyt navn")
            input(type="hidden", name="chatId", value=chat.id)
            button(type="submit") Redigér 