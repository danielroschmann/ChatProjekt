doctype HTML 
html 
    head 
        title Chats 
        script(src="index.js") 
        link(rel="stylesheet", href="/css/chats.css")
        link(rel="stylesheet", href="css/menubar.css")
    body 
        include includes/menubar
        h1 Chats
        ul 
            if (chats !== undefined) 
                if (chats.length > 0)
                    each chat in chats 
                        li 
                          a(href=`/chats/${chat.id}`, style="display: block; width: 100%; height: 100%") #{chat.navn}
                else 
                    h1 Der er ingen chats tilrådighed

            